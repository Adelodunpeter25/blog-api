[project]
name = "blog-api"
version = "0.1.0"
description = "Django REST Framework Blog API"
requires-python = ">=3.8"
dependencies = [
    "django>=4.2.0",
    "djangorestframework>=3.14.0",
    "dotenv>=0.9.9",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-django",
]
